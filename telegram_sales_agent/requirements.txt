telethon>=1.34
openai>=1.0
python-dotenv>=1.0
httpx>=0.27
# Нужен Telethon для SOCKS5-прокси (TG_PROXY_* в .env) — см. manager_pool.py.
PySocks>=1.7

# Нужен только для продакшена (Google Sheets). Для локального теста
# используется local_sheet.py и этот пакет не требуется.
gspread>=6.0

# Опционально, только если запускаете панель управления app_streamlit.py.
# streamlit>=1.30

# Тестирование (не нужно в проде)
pytest>=8.0
pytest-asyncio>=0.23
