# Скопируйте в .env.production на сервере и заполните реальными значениями.
# Этот файл — только шаблон, .env.production в git не коммитится.

DATABASE_URL="postgresql://autogent:CHANGE_ME@db:5432/autogent"
NEXTAUTH_SECRET="CHANGE_ME_RANDOM_32_BYTES"
NEXTAUTH_URL="https://crm.autogentgroup.ru"

# Пароль для сервиса db в docker-compose.prod.yml (POSTGRES_PASSWORD)
POSTGRES_PASSWORD="CHANGE_ME"
